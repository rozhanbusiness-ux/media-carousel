services:
  media-carousel-app:
    build: .
    container_name: media-carousel-app
    restart: unless-stopped
    ports:
      - "3010:3000"
    env_file:
      - .env
    environment:
      - PORT=3000
    volumes:
      - ./output:/app/output
      - ./cache:/app/cache
